## IT Logger

Project created using React and Redux.

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).


### Development

Use `npm start` to start dev server.

The server will open on localhost:3000 or the local enviroment variable to development server.

